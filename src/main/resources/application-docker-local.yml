server:
  port: 8088

spring:
  datasource:
    url: jdbc:postgresql://localhost:5437/task-management
    username: postgres
    password: pass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true